### 1. Isolation Forest: "Urgent" 항목 점검 ()

| 항목 | 상태 | 보완이 필요한 빈틈 (Gaps) |
| :--- | :--- | :--- |
| **왜 IF인가?** | **미흡** | 코드에는 IF만 있지만, README에는 **Autoencoder(대량 데이터 필요/학습 무거움)**나 **One-Class SVM(느림)** 대비 IF가 **"실시간 스코어링과 비지도 학습에 왜 최적인지"** 비교 데이터가 명시되어야 합니다. |
| **학습 데이터** | **진행중** | 현재 3개 피처를 썼는데, 멘토님은 **"응답 코드 분포"**나 **"페이로드 길이"** 같은 더 다양한 피처를 언급하셨습니다. 또한 "며칠간 수집된 몇 건의 데이터인가?"에 대한 정량적 수치가 필요합니다. |
| **하이퍼파라미터** | **수정 필요** | 현재 `contamination=0.15`를 썼는데, 멘토님은 **0.05**를 예시로 드셨습니다. "왜 0.15인가?"에 대해 **"테스트 환경에서 공격 밀도가 높게 설정되었기 때문"**이라는 방어 논리가 필요합니다. |
| **평가 결과** | **완료** | 29건의 탐지 결과와 Precision/Recall 산출 로직을 갖췄으므로 이 부분은 통과입니다. |
| **한계 인정** | **미흡** | "이 모델이 모든 공격을 다 잡는다"가 아니라, **"패턴이 없는 제로데이 공격은 잡지만, 정상 요청과 구분이 안 되는 저속(Low-and-slow) 공격에는 취약할 수 있다"**는 식의 정직한 기술이 필요합니다. |

---

### 2. 기타 기술 선택 근거 (아직 손대지 않은 부분) ()

이 부분들은 오늘 코드 작업에는 없었지만, 멘토님이 **"근거 부족"**으로 찍으신 항목들입니다.

*   **Athena**: 단순히 쓴 게 아니라, **CloudWatch Logs Insights** 대비 왜 좋은지(S3 직접 쿼리, 비용 효율성)를 써야 합니다.
*   **EventBridge + Lambda**: **AWS Step Functions**라는 대안이 있는데 왜 이 조합을 썼는지(단순 이벤트 트리거에는 오버헤드가 적은 이 방식이 적합하다는 등)가 빠져 있습니다.
*   **tfsec**: **Checkov**나 **Terrascan** 대신 왜 이걸 골랐는지에 대한 비교가 필요합니다.

---

### 3. [최우선 보완] WAF 룰 개선 "의사결정 과정" ()

멘토님이 가장 강조하신 부분 중 하나입니다. 단순히 "룰을 고쳤다"가 아니라 다음 흐름으로 스토리를 짜야 합니다.

1.  **문제(Problem)**: 기존 WAF 룰에서 탐지하지 못하는 변조된 XSS 패턴 발견 (예: 인코딩된 구문).
2.  **분석(Analysis)**: 정규표현식 기반의 한계와 AI 탐지의 필요성 도출.
3.  **개선(Improvement)**: 오늘 작업한 Isolation Forest 모델을 결합하여 탐지율을 14건에서 29건으로 향상.

---

### 투두 리스트

1.  **README 업데이트**: 위에서 말한 "왜(Why)"에 대한 답변들을 텍스트로 정리하세요. 
2.  **피처 다양화 (선택사항)**: `eval_model.py`에 **'URI 엔트로피'**나 **'응답 데이터 크기'** 같은 피처를 한두 개 더 추가하면 멘토님이 "꼼꼼하게 피드백 반영했네?"라고 생각하실 겁니다.
3.  **하이퍼파라미터 주석**: `train_model.py` 코드에 `contamination=0.15`를 쓴 이유를 주석으로 명확히 달아두세요. 

> "현재 시뮬레이션 환경의 공격 로그 비중이 약 15%로 설계되어 해당 값을 설정함. 실제 운영 환경에서는 0.01~0.05 사이로 최적화 예정."

**참고한 자료**
*   Isolation Forest 모델 상세 섹션 및 평가 체계 필요성
*   기술별 선택 근거(Athena, tfsec 등) 보완 가이드
*   Isolation Forest 하이퍼파라미터 및 피처 엔지니어링 개선 방향
---
##  Phase 1: AI 엔진 고도화 및 정량적 근거 확보

### 1. 신규 피처: Shannon Entropy 도입 (무작위성 탐지)
단순 특수문자 개수보다 정교한 지표입니다. 공격자가 탐지를 피하려고 페이로드를 복잡하게 짤수록 엔트로피 수치가 올라갑니다.

*   **수식**: $$H(X) = -\sum_{i=1}^{n} P(x_i) \log_2 P(x_i)$$
*   **의미**: URI 내 문자 분포의 불확실성을 측정합니다. 정상적인 `/index.html`보다 SQL Injection 구문이 포함된 URI의 엔트로피가 훨씬 높게 나타납니다.
*   **실행**: `scripts/train_model.py`와 `scripts/analyze_anomalies.py`에 아래 함수를 추가하여 피처를 3개에서 **4개**로 확장하세요.

```python
import math
from collections import Counter

def calculate_entropy(s):
    """URI의 복잡도를 수치화하여 난독화된 공격을 식별합니다."""
    if not s: return 0
    prob = [n_x / len(s) for n_x in Counter(s).values()]
    return -sum(p * math.log2(p) for p in prob)
```

---

### 2. 하이퍼파라미터 의사결정 매트릭스 (면접/보고용)

*   **`contamination=0.15`**:
    *   **근거**: 현재 확보된 테스트 데이터셋(Ground Truth) 내 공격 로그 비중이 약 14.8%임. 모델이 이를 '이상치'로 인식하게 강제하여 **Recall(재현율)을 1.0**에 가깝게 맞추기 위한 전략적 설정임.
    *   **운영 계획**: 실운영 환경(Production)에서는 WAF의 정상 트래픽 비중이 압도적이므로 `0.01~0.05`로 하향 조정하여 오탐(False Positive)을 최소화할 예정임.
*   **`n_estimators=100`**:
    *   **근거**: 50개일 때보다 결정 경계가 안정적이며, 200개 이상일 때보다 메모리 점유율 및 추론 속도가 빨라 **실시간 보안 관제**에 최적화된 수치임.

---

### 3. 기술 비교 분석 (Why Isolation Forest?)

| 기술 | 장점 | 단점 | 선택 이유 |
| :--- | :--- | :--- | :--- |
| **Isolation Forest** | **빠른 속도, 선형 복잡도** | 전역적 이상치에 최적화 | **WAF 로그 같은 대용량 실시간 분석에 가장 적합** |
| Autoencoder | 정교한 특징 추출 가능 | 학습 리소스 과다, 튜닝 어려움 | 인프라 비용 및 실시간 응답성 면에서 불리함 |
| One-Class SVM | 비선형 경계 잘 잡음 | 데이터 커지면 연산 속도 급락 | 대규모 로그 처리 시 병목 현상 발생 우려 |

---

##  Phase 2: 인프라 표준화 및 자동화 (Docker & SOAR)

### 1. Docker 환경 격리 (의존성 해결)
WSL에서 돌아가던 환경을 그대로 컨테이너에 담습니다. `numpy`, `pandas` 버전 충돌을 원천 차단합니다.

**`Dockerfile` 작성 예시:**
```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# 29건 탐지 리포트를 바로 생성하도록 설정
CMD ["python", "scripts/analyze_anomalies.py"]
```

### 2. AWS SOAR 파이프라인 설계 (3단계 고도화)
탐지(AI)에서 차단(WAF)까지의 연결 고리입니다.

1.  **Detection**: `analyze_anomalies.py`가 위협 IP 리스트 추출.
2.  **Trigger**: 결과를 S3에 JSON으로 업로드 시 S3 Event Notification 발생.
3.  **Action (Lambda)**: 
    *   `XSS`, `Admin Access` 등급에 따라 차단 시간(TTL) 결정.
    *   **AWS WAF v2 UpdateIPSet API**를 호출하여 즉시 차단.

---

##  Phase 3: 가시성 및 평가 자동화 (4단계)

### 1. 정량적 성적표 자동 생성
`eval_model.py`를 실행할 때마다 `ai-security/results/performance_metrics.json`이 생성되게 하세요.

```python
# scripts/eval_model.py 하단에 추가
import json
from sklearn.metrics import classification_report

report = classification_report(y_true, y_pred, output_dict=True)
with open('results/performance_metrics.json', 'w') as f:
    json.dump(report, f, indent=4)
```

### 2. 한계 인정 및 보완 계획 (Honest Engineering)
*   **한계**: Isolation Forest는 "분포가 다른 것"을 잡는 것이지 "악의적인 의도"를 직접 이해하는 것이 아님. 따라서 정상 사용자의 매우 긴 URI 요청을 오탐할 가능성이 있음.
*   **보완**: 이를 방지하기 위해 **AWS WAF Managed Rules(정적 분석)**로 명확한 공격을 1차 필터링하고, AI는 2차적으로 **변조된 지능형 공격**을 잡아내는 **Hybrid 체계**로 운영함.

---
