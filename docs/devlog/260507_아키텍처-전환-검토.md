# [260507] 아키텍처 전환 검토 — WAF+ALB+EC2 vs WAF+APIGW+Lambda

## 1. 배경

멘토님 가이드라인(WAF + ALB + EC2)을 실습 비용 절감 및 서버리스 경험 확보 목적으로
WAF + API Gateway + Lambda 구조로 변경하여 배포 시도.
오늘 현재 상태 점검 및 방향 재확인.

---

## 2. 현재 상태 점검

| 항목 | 상태 |
|------|------|
| AWS 실제 리소스 (ALB, WAF) | 존재함 (CLI 삭제 시도했으나 실제 삭제 안 됨, 콘솔 확인) |
| Terraform state | ALB, WAF, EC2 모두 기록되어 있음 |
| Terraform 코드 | ALB 모듈 + EC2 + apigateway.tf 혼재 |
| WAF 연결 대상 | ALB (API Gateway에는 아직 미연결) |

**결론**: state drift 없음. 코드와 AWS 리소스 일치. 단, 두 아키텍처가 코드에 혼재 중.

### 교훈
AWS 리소스를 CLI로 직접 삭제하면 Terraform state와 불일치(drift) 발생.
앞으로는 `terraform destroy -target` 또는 코드 수정 → `terraform apply` 순서로 관리.

---

## 3. 아키텍처 전환 트레이드오프

### 얻는 것 (API Gateway + Lambda)
- EC2 t3.micro 월 $8-9 고정 비용 → Lambda는 포트폴리오 트래픽 기준 거의 $0
- OS 패치, AMI 관리 등 운영 오버헤드 제거
- WAF 로그 → S3 → Lambda 트리거 흐름이 이벤트 기반 워크로드에 자연스러움

### 포기하는 것 (ALB + EC2)
- ALB L7 헬스체크, 고급 라우팅 기능
- 전통적인 서버 보안 하드닝 실습 경험
- 구조 단순성 (WAF → ALB → EC2 직선 흐름)

### 이 프로젝트에서 Lambda가 적합한 이유
핵심 워크로드가 WAF 로그 분석 + Isolation Forest 스코어링으로,
상시 실행 서버보다 이벤트 기반 처리가 더 자연스러운 구조.

---

## 4. 프로젝트 핵심 방향 재확인

이 프로젝트의 차별화 포인트는 **인프라가 아닌 AI 레이어**:

- Isolation Forest 기반 이상 탐지 (실제 Nikto 공격 로그 450건)
- Weighted FedAvg — 데이터 불균형 환경에서 Privacy-Preserving 모델 집계
- 데이터 품질 진단 (Sentinel 값 왜곡 인식 및 기록)

---

## 5. 우선순위 재정립

| 우선순위 | 작업 |
|---------|------|
| 높음 | Shannon Entropy 피처 추가 (`train_model.py`, `analyze_anomalies.py`) |
| 높음 | Data Cleaning 파이프라인 (Sentinel 값 필터링) |
| 중간 | CI/CD 연동 (GitHub Actions 모델 테스트 자동화) |
| 낮음 | 인프라 정리 (ALB 코드 제거, WAF → API Gateway 연결) |

인프라 구조 확정은 AI 작업 이후에 한 번에 정리.

---

## 6. 문서화 방식 결정

| 내용 | 위치 |
|------|------|
| 아키텍처 결정, 트러블슈팅 원인/결과, 코드 변경 근거 | GitHub 레포 (`/docs/devlog/`) |
| 스크린샷, 콘솔 캡처, 실행 결과 화면 | 노션 |

---

## 7. 다음 작업

- [ ] Shannon Entropy 피처 구현
- [ ] Sentinel 값 필터링 전처리 파이프라인 구축
- [ ] 인프라 방향 최종 확정 후 코드 정리
