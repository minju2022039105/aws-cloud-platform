## [260520] Velog 6편 초안 완성 + 아키텍처 문서화

---

### 작업 내용

**Velog 6편 초안 작성 완료** (`docs/blog/06_SOAR-파이프라인-AI-대시보드.md`)
- SOAR 파이프라인 완성 과정 3단계 트러블슈팅 서술 (Athena 테이블 누락 → IP Set ID 불일치 → IAM 권한)
- monitor.py → Prometheus → Grafana Cloud 연결 실패 이유와 S3/Athena 전환 결정 근거 포함
- AI 추가 탐지 146건을 "공격"이 아닌 "이상 징후 후보"로 표현한 이유 명시 (WAF 레이블 기반 평가 한계)

**아키텍처 문서 작성** (`docs/architecture/260519_soar_pipeline_architecture.md`)
- SOAR 파이프라인 전체 흐름 다이어그램
- Athena 테이블 3종 정리 (`aiops_results` / `model_metrics` / `waf_logs`)
- Terraform 모듈 참조 구조 및 IAM 권한 매핑

**Grafana AI 대시보드 스크린샷 추가** (`docs/devlog/image/grafana.png`)

---

### Grafana Stat 패널 수치 전환 처리

Athena `model_metrics` 테이블에 저장된 값은 0~1 범위 소수(예: `precision = 0.5797`).
Grafana Stat 패널에서 "%" 단위로 표시하려면 Unit 설정만으로 부족 — SQL에서 ×100 변환 필요.

```sql
SELECT precision * 100 AS value FROM default.model_metrics;
SELECT recall    * 100 AS value FROM default.model_metrics;
SELECT f1_score  * 100 AS value FROM default.model_metrics;
SELECT fpr       * 100 AS value FROM default.model_metrics;
```

Grafana Unit은 `None` (혹은 `percent (0-100)`)으로 설정하고, 소수점 2자리까지만 표시.

---

### 어제(260519) 작업 devlog 정리

260519 devlog에 누락됐던 내용 보완:
- Grafana AI 대시보드 패널 전체 구성 및 Athena 쿼리 추가
- SOAR 파이프라인 최종 확정 다이어그램 추가

---

### 오늘 기준 전체 산출물 상태

| 항목 | 상태 |
|------|------|
| SOAR 파이프라인 end-to-end | ✅ 완성 |
| Grafana WAF 대시보드 | ✅ 완성 |
| Grafana AI 대시보드 | ✅ 완성 |
| Velog 6편 초안 | ✅ 작성 완료 (이미지 삽입 후 발행 예정) |
| 아키텍처 문서 | ✅ 완성 |

---

### 내일 할 일

- [ ] Velog 6편 이미지 삽입 후 발행
- [ ] README 전체 업데이트 (SOAR 파이프라인 섹션 + AI 대시보드 링크)
- [ ] 전체 커밋 정리 및 push
