# AVD-AWS-0038: EKS endpoint_public_access = true
# 포트폴리오/학습 환경 — 집 IP 유동으로 CIDR 제한 불가. 실 운영 시 false 권장.
AVD-AWS-0038

# AVD-AWS-0107: Security group ingress from 0.0.0.0/0
# CI에서 TF_VAR_my_ip=0.0.0.0/0 사용. 실 운영 시 고정 CIDR 적용.
AVD-AWS-0107

# AVD-AWS-0104: Security group egress to 0.0.0.0/0
# AWS API / apt 등 외부 통신 필요. VPC Flow Logs로 모니터링.
AVD-AWS-0104

# AVD-AWS-0041: EKS control plane logging disabled
# 포트폴리오/학습 환경 — CloudWatch 로그 비용 절감. 실 운영 시 활성화 권장.
AVD-AWS-0041

# AVD-AWS-0039: EKS secrets encryption not configured
# 포트폴리오/학습 환경 — KMS 암호화 비용 절감. 실 운영 시 encryption_config 추가 권장.
AVD-AWS-0039

# AVD-AWS-0040: EKS endpoint_public_access = true
# 포트폴리오/학습 환경 — WSL 로컬 kubectl 접근 필요. 실 운영 시 false 권장.
AVD-AWS-0040
