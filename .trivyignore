# AVD-AWS-0038: EKS endpoint_public_access = true
#
# 허용 사유: 포트폴리오/학습 환경에서 WSL 로컬 머신으로 kubectl 직접 접근이 필요함.
# 집 IP가 유동이라 public_access_cidrs를 고정 CIDR로 제한하기 어려운 상황.
# 실 운영 환경에서는 endpoint_public_access = false 또는 CIDR 제한 적용 권장.
AVD-AWS-0038
